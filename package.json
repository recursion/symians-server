{
  "name": "symians",
  "version": "0.0.1",
  "description": "An es6 websocket server.",
  "main": "index.js",
  "scripts": {
    "serve-dev": "nodemon --watch src --exec npm run serve",
    "test-dev": "mocha --watch --opts ./mocha.opts src/**/*.spec.js",
    "serve": "node -- index.js",
    "test": "mocha --opts ./mocha.opts src/**/*.spec.js",
    "lint": "eslint ./src/**/*.js; exit 0",
    "pretest": "npm run lint",
    "preserve": "npm run test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^5.8.23",
    "express": "^4.13.3",
    "socket.io": "^1.3.7"
  },
  "devDependencies": {
    "babel-eslint": "^4.1.3",
    "chai": "^3.3.0",
    "eslint": "^1.6.0",
    "mocha": "^2.3.3",
    "require-directory": "^2.1.1"
  }
}
